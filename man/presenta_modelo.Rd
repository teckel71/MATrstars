% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/presenta_modelo.R
\name{presenta_modelo}
\alias{presenta_modelo}
\title{Presentar los resultados de un modelo lineal}
\usage{
presenta_modelo(
  modelo,
  captions = c("Modelo Lineal", "Estadísticos del modelo",
    "Factor de inflación de la varianza"),
  ...
)
}
\arguments{
\item{modelo}{Objeto de modelo lineal ajustado (habitualmente devuelto por
\code{\link[stats:lm]{stats::lm()}}). Debe ser compatible con \code{\link[broom:reexports]{broom::tidy()}}, \code{\link[broom:reexports]{broom::glance()}}
y \code{\link[car:vif]{car::vif()}}. Este ultimo requiere que el modelo tenga al menos dos
variables explicativas.}

\item{captions}{Vector de caracteres de longitud 3 con los titulos de las
tres tablas, en orden: coeficientes, estadisticos, VIF. Por defecto,
\code{c("Modelo Lineal", "Estad\u00edsticos del modelo",
  "Factor de inflaci\u00f3n de la varianza")}.}

\item{...}{Argumentos adicionales pasados a \code{\link[=kable_rstars]{kable_rstars()}} (por ejemplo,
\code{font_size} o \code{bootstrap_options}), aplicados por igual a las tres tablas.}
}
\value{
Lista de tres objetos \code{knitr_kable} con nombres \code{coef}, \code{stats} y
\code{vif}, accesibles tanto por posicion (\code{x[[1]]}) como por nombre
(\code{x$coef}).
}
\description{
Convierte el objeto devuelto por \code{\link[stats:lm]{stats::lm()}} (u otros modelos compatibles
con \code{\link[broom:reexports]{broom::tidy()}} y \code{\link[broom:reexports]{broom::glance()}}) en un conjunto de tres tablas
formateadas con \code{\link[=kable_rstars]{kable_rstars()}}, listas para su inclusion en un documento
R Markdown con salida HTML. Las tres tablas son:
}
\details{
\enumerate{
\item \strong{Coeficientes}: estimaciones, errores estandar, estadistico t,
p-valor y niveles de significacion (columna \code{stars}).
\item \strong{Estadisticos del modelo}: R^2, R^2 ajustado, sigma, estadistico F,
p-valor global, AIC y numero de observaciones.
\item \strong{Factor de inflacion de la varianza (VIF)}: valores de VIF para
cada variable explicativa del modelo.
}

Internamente, cada una de las tres tablas se construye con
\code{\link[=kable_rstars]{kable_rstars()}}, por lo que hereda el estilo tipografico habitual del
libro. Cualquier argumento adicional se reenvia a \code{\link[=kable_rstars]{kable_rstars()}} mediante
\code{...}, aplicandose por igual a las tres tablas.
}
\examples{
\dontrun{
modelo <- lm(mpg ~ wt + hp + disp, data = mtcars)
salida <- presenta_modelo(modelo)

salida[[1]]   # tabla de coeficientes
salida[[2]]   # tabla de estadisticos
salida[[3]]   # tabla de VIF

# Personalizar titulos y tamano de fuente:
presenta_modelo(modelo,
                captions  = c("Coeficientes", "Ajuste global", "VIF"),
                font_size = 12)
}

}
