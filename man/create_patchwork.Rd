% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/create_patchwork.R
\name{create_patchwork}
\alias{create_patchwork}
\title{Componer una lista de ggplots en retículas 2x2 con patchwork}
\usage{
create_patchwork(plot_list)
}
\arguments{
\item{plot_list}{Lista de objetos \code{ggplot}. Si la lista está vacía la
función devuelve \code{NULL}.}
}
\value{
Una lista de composiciones \code{patchwork}. Cada elemento contiene
como máximo cuatro gráficos dispuestos en una retícula 2x2. Para
imprimir las composiciones basta con recorrer la lista y llamar a
\code{print()} sobre cada elemento.
}
\description{
Toma una lista de objetos \code{ggplot} y los agrupa en composiciones de dos
filas por dos columnas (cuatro gráficos por composición) usando los
operadores de \pkg{patchwork}. Si el número total de gráficos no es
múltiplo de cuatro, la última composición se completa con paneles vacíos
(\code{theme_void()}) para mantener la retícula uniforme.
}
\details{
Esta función se utiliza en varios capítulos del libro \emph{R-Stars} para
presentar de forma ordenada baterías de gráficos: centroides por clúster,
comparaciones variable-variable, análisis por grupos, etc.
}
\examples{
\dontrun{
library(ggplot2)
library(patchwork)

# Lista de 5 gráficos de ejemplo
graficos <- lapply(1:5, function(i) {
  ggplot(mtcars, aes(x = wt, y = mpg)) +
    geom_point() +
    ggtitle(paste("Gráfico", i))
})

composiciones <- create_patchwork(graficos)

for (comp in composiciones) print(comp)
}

}
