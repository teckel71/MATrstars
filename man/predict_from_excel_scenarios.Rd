% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/predict_from_excel_scenarios.R
\name{predict_from_excel_scenarios}
\alias{predict_from_excel_scenarios}
\title{Predecir por escenarios definidos en Excel}
\usage{
predict_from_excel_scenarios(
  model,
  train_df,
  excel_path,
  sheet = "Escenarios",
  id_col = "ESCENARIO",
  eval_dfs = list(),
  interval = c("prediction", "confidence"),
  level = 0.95,
  transforma_log = FALSE,
  return_kable = TRUE,
  caption = "Predicciones por escenario",
  ...
)
}
\arguments{
\item{model}{Objeto de modelo ajustado (por ejemplo, devuelto por
\code{\link[stats:lm]{stats::lm()}}).}

\item{train_df}{Data frame de entrenamiento del modelo. Se utiliza para
determinar los niveles de las variables factor y para resolver las
referencias a variables sin prefijo dentro de las formulas de Excel.
Ademas, queda disponible bajo el alias \code{train} en dichas formulas.}

\item{excel_path}{Ruta al archivo Excel con los escenarios.}

\item{sheet}{Nombre o indice de la hoja del Excel. Por defecto,
\code{"Escenarios"}.}

\item{id_col}{Nombre de la columna del Excel que identifica cada escenario.
Por defecto, \code{"ESCENARIO"}. Si la columna no existe en la hoja, se crea
con un identificador correlativo.}

\item{eval_dfs}{Lista con nombre de data frames adicionales que se quieren
exponer, bajo esos nombres, a las formulas escritas en las celdas de
Excel. Por ejemplo, \code{list(df1 = seleccion)} permite escribir en Excel
\code{df1$IVENTAS}. Por defecto, \code{list()} (solo \code{train_df} bajo el alias
\code{train}).}

\item{interval}{Tipo de intervalo devuelto por \code{\link[stats:predict.lm]{stats::predict.lm()}}:
\code{"prediction"} (por defecto) o \code{"confidence"}.}

\item{level}{Nivel de confianza del intervalo. Por defecto \code{0.95}.}

\item{transforma_log}{Si \code{TRUE}, aplica \code{exp()} a las predicciones y a los
extremos del intervalo. Util cuando el modelo estima \code{log(y)} pero se
desea presentar los resultados en la escala original de \code{y}. La
desviacion tipica se transforma mediante el metodo delta aproximado. Por
defecto, \code{FALSE}.}

\item{return_kable}{Si \code{TRUE} (por defecto), devuelve ademas una tabla
formateada con \code{\link[=kable_rstars]{kable_rstars()}} en el elemento \verb{$table} de la lista de
salida. Si \code{FALSE}, ese elemento vale \code{NULL}.}

\item{caption}{Titulo base de la tabla formateada. La funcion lo completa
automaticamente anadiendo el nombre de la variable dependiente, el tipo
de intervalo, el nivel de confianza y, si procede, una indicacion de que
los valores estan en la escala original tras antilog. Por defecto,
\code{"Predicciones por escenario"}.}

\item{...}{Argumentos adicionales pasados a \code{\link[=kable_rstars]{kable_rstars()}} (por ejemplo,
\code{digits}, \code{font_size} o \code{bootstrap_options}). Si no se pasa \code{digits}, se
aplica un valor por defecto de \code{3}.}
}
\value{
Una lista con dos elementos:
\itemize{
\item \code{data}: data frame con una fila por escenario, incluyendo la
columna identificadora, las variables explicativas evaluadas, la
prediccion puntual (\code{.fitted}), los extremos del intervalo
(\code{.lwr}, \code{.upr}), la desviacion tipica de la prediccion
(\code{.se_fit}) y los metadatos \code{.interval}, \code{.level} y
\code{.log_transform}.
\item \code{table}: objeto \code{knitr_kable} con el data frame anterior
formateado con \code{\link[=kable_rstars]{kable_rstars()}} (o \code{NULL} si \code{return_kable = FALSE}).
}
}
\description{
Lee escenarios de simulacion o prevision desde una hoja de un archivo Excel,
donde cada celda puede contener un valor numerico o una formula de R (por
ejemplo, \code{mean(train$ACTIVO)} o \code{df1$IVENTAS[1]}), y devuelve las
predicciones del modelo para cada escenario. Opcionalmente, presenta los
resultados en una tabla formateada con \code{\link[=kable_rstars]{kable_rstars()}}.
}
\details{
Utilidad principal: en las practicas del capitulo 9 (regresion lineal
multiple) permite al alumno definir en Excel varios escenarios de prediccion
sin necesidad de codificarlos en R, incluyendo formulas que referencian
variables de los data frames del script.

Las formulas en las celdas de Excel se evaluan en un entorno donde:
\itemize{
\item cada variable de \code{train_df} esta disponible por su nombre (por
ejemplo, \code{mean(ACTIVO)});
\item el propio \code{train_df} esta disponible bajo el alias \code{train} (salvo
que \code{eval_dfs} lo sobrescriba);
\item cualquier data frame adicional pasado por \code{eval_dfs} esta
disponible bajo el nombre que se le asigne en la lista.
}
}
\examples{
\dontrun{
# Estimacion de un modelo log-lineal
modelo <- lm(log(mpg) ~ wt + hp, data = mtcars)

# Predicciones desde un archivo Excel con escenarios
res <- predict_from_excel_scenarios(
  model          = modelo,
  train_df       = mtcars,
  excel_path     = "escenarios.xlsx",
  sheet          = "Simula1",
  transforma_log = TRUE
)

res$table    # tabla formateada
res$data     # data frame crudo
}

}
