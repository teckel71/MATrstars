% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/generar_solucion.R
\name{generar_solucion}
\alias{generar_solucion}
\title{Generar tablas y grafico a partir de un modelo log-lineal}
\usage{
generar_solucion(
  modelo,
  captions = c("Validación del modelo", "Coeficientes del modelo"),
  mosaic_main = "Residuos del modelo",
  ...
)
}
\arguments{
\item{modelo}{Objeto de modelo log-lineal ajustado (habitualmente
devuelto por \code{\link[MASS:loglm]{MASS::loglm()}}).}

\item{captions}{Vector de caracteres de longitud 2 con los titulos de
las dos tablas, en orden: validacion y coeficientes. Por defecto,
\code{c("Validaci\\u00f3n del modelo", "Coeficientes del modelo")}.}

\item{mosaic_main}{Titulo del grafico de mosaico. Por defecto,
\code{"Residuos del modelo"}.}

\item{...}{Argumentos adicionales pasados a \code{\link[=kable_rstars]{kable_rstars()}} (por
ejemplo, \code{font_size} o \code{bootstrap_options}), aplicados por igual a
las dos tablas. Si no se pasa \code{digits}, se aplica un valor por
defecto de \code{3}.}
}
\value{
Lista con dos elementos:
\itemize{
\item \code{Informacion}: objeto \code{knitr_kable} con la tabla de las
pruebas de bondad de ajuste.
\item \code{Coeficientes}: objeto \code{knitr_kable} con la tabla de
coeficientes del modelo.
}
}
\description{
Automatiza la presentacion de resultados de un modelo log-lineal
ajustado (habitualmente devuelto por \code{\link[MASS:loglm]{MASS::loglm()}}). A partir del
modelo, la funcion:
}
\details{
\enumerate{
\item Extrae las pruebas de bondad de ajuste (razon de verosimilitud
y ji-cuadrado de Pearson) y las presenta en una tabla
formateada con \code{\link[=kable_rstars]{kable_rstars()}}.
\item Extrae los coeficientes del modelo mediante
\code{\link[=extraer_coeficientes]{extraer_coeficientes()}} y los presenta en una segunda tabla
formateada con \code{\link[=kable_rstars]{kable_rstars()}}.
\item Dibuja un grafico de mosaico con los residuos del modelo
(usando \code{\link[vcd:mosaic]{vcd::mosaic()}} con sombreado \code{\link[vcd:shadings]{vcd::shading_hcl()}})
en el dispositivo grafico activo.
}

Las dos tablas se devuelven en una lista de dos elementos:
\code{Informacion} y \code{Coeficientes}. El grafico de mosaico es un efecto
colateral del renderizado grafico y no forma parte del valor de
retorno.
}
\note{
Esta funcion sustituye a la version anterior (previa a la
integracion en \code{MATrstars}) que asignaba la lista de resultado en
el Global Environment mediante \code{assign(..., envir = .GlobalEnv)}.
El nuevo comportamiento requiere que el usuario capture
explicitamente el valor devuelto:
\code{solucion <- generar_solucion(modelo)}.
}
\section{Coeficientes no finitos}{

Cuando la tabla de contingencia de entrada contiene celdas de
frecuencia 0, algunos coeficientes del modelo pueden resultar
\code{Inf}, \code{-Inf} o \code{NaN}. Esto no es un error: es un fenomeno esperable
derivado de que el estimador de maxima verosimilitud del modelo
saturado no existe en sentido estricto sobre tablas con ceros
muestrales (los parametros "se escapan al infinito" en el borde del
espacio parametrico). La funcion detecta esta situacion y emite un
\code{message()} informativo, pero no aborta ni imputa. El modelo sigue
siendo utilizable como punto de partida para \code{\link[stats:step]{stats::step()}}, que
opera sobre la desvianza (finita, valor 0 para el saturado) y no
sobre los coeficientes patologicos. Vease Agresti (2013), cap. 9, y
Fienberg (2007), cap. 5, para el tratamiento formal.
}

\examples{
\dontrun{
modelo_indep <- MASS::loglm(~ GALAXIA + FJUR + EFLO, data = tab_use)
solucion <- generar_solucion(modelo_indep)

solucion$Informacion    # tabla de validacion
solucion$Coeficientes   # tabla de coeficientes
# (el mosaico se dibuja automaticamente al llamar a la funcion)
}

}
