% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/explora_na.R
\name{explora_na}
\alias{explora_na}
\title{Explora (y opcionalmente elimina) valores ausentes.}
\usage{
explora_na(
  datos,
  variables = NULL,
  accion = c("documentar", "eliminar"),
  titulo = "Valores ausentes",
  subtitulo = NULL,
  etiqueta_y = "Observación",
  caption = NULL,
  ...
)
}
\arguments{
\item{datos}{Data frame de entrada.}

\item{variables}{Variables a analizar (tidy-select). Si se omite, se
analizan todas las columnas de \code{datos}.}

\item{accion}{\code{"documentar"} (por defecto) solo diagnostica.
\code{"eliminar"} ademas filtra los casos con algun \code{NA}
en las variables analizadas.}

\item{titulo}{Titulo del grafico. Por defecto, \code{"Valores ausentes"}.}

\item{subtitulo}{Subtitulo del grafico. Por defecto, \code{NULL}.}

\item{etiqueta_y}{Etiqueta del eje Y. Por defecto, \code{"Observacion"}.}

\item{caption}{Titulo de la tabla \code{kable_rstars()} con los casos con
\code{NA}. Si es \code{NULL} (por defecto), se genera
automaticamente.}

\item{...}{Argumentos adicionales reenviados a \code{\link[=kable_rstars]{kable_rstars()}}
(por ejemplo, \code{digits}, \code{font_size} o
\code{bootstrap_options}).}
}
\value{
El data frame (invisible): sin cambios si
\code{accion = "documentar"}, o filtrado (solo casos completos en las
variables analizadas) si \code{accion = "eliminar"}.
}
\description{
Diagnostica los valores ausentes (\code{NA}) de un data frame sobre un
conjunto de variables. Genera un grafico \code{\link[visdat:vis_miss]{visdat::vis_miss()}} con el
estilo del libro y, si hay \code{NA}s, una tabla con los casos afectados
formateada con \code{\link[=kable_rstars]{kable_rstars()}}. Si no se detecta ningun \code{NA}, la
funcion solo emite un mensaje informativo. Opcionalmente, elimina del
data frame los casos con algun \code{NA} en las variables analizadas.
}
\details{
La seleccion de variables sigue el estilo \emph{tidy-select} del \emph{tidyverse}:
se pueden pasar los nombres sin comillas, con \code{c()}, o con los \emph{helpers}
de \code{\link[dplyr:select]{dplyr::select()}} (por ejemplo, \code{starts_with("I")}). Si no se
especifica ninguna variable, se analizan todas las columnas del data
frame.

Cuando el numero de variables analizadas es reducido (hasta 4), la
funcion corrige la posicion por defecto de las etiquetas del eje x
superior en el grafico de \code{vis_miss()}, que en ese caso quedan muy
separadas del panel. Para mas de 4 variables, mantiene el angulo de 45
grados habitual.
}
\examples{
\dontrun{
  library(MATrstars)

  # Una sola variable (sin comillas):
  muestra <- explora_na(
    muestra, RENECO,
    accion    = "eliminar",
    titulo    = "Rentabilidad Economica: valores ausentes",
    subtitulo = "Transporte de mercancias interestelar"
  )

  # Varias variables (sin comillas):
  muestra2 <- explora_na(
    muestra2, c(RENECO, ACTIVO, MARGEN, RES),
    accion = "eliminar"
  )

  # Todo el data frame (sin argumento `variables`):
  seleccion <- explora_na(seleccion, accion = "eliminar")
}

}
