% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/impute_zeros_with_one.R
\name{impute_zeros_with_one}
\alias{impute_zeros_with_one}
\title{Imputar celdas con frecuencia cero de una tabla de contingencia}
\usage{
impute_zeros_with_one(tab, value = 1L)
}
\arguments{
\item{tab}{Tabla de contingencia (objeto \code{table}, \code{array} o \code{matrix})
de cualquier numero de dimensiones.}

\item{value}{Valor con el que sustituir las celdas con frecuencia cero.
Por defecto, \code{1L}.}
}
\value{
Una tabla del mismo tipo y dimensiones que \code{tab}, con los
ceros sustituidos por \code{value} y con los atributos \code{zeros_imputados}
y \code{nota_imputacion} adjuntos.
}
\description{
Reemplaza en una tabla de contingencia los ceros por un valor minimo
(por defecto, \code{1L}), preservando el resto de las celdas (nunca modifica
celdas con frecuencia positiva). El objetivo es preparar la tabla para
la estimacion de modelos log-lineales, cuyo procedimiento puede fallar
o producir estimaciones inestables en presencia de ceros.
}
\details{
La tabla devuelta lleva dos atributos que documentan la operacion:
\itemize{
\item \code{zeros_imputados}: data frame identico al devuelto por
\code{\link[=detect_zeros_any]{detect_zeros_any()}}, con una columna adicional
\code{Frecuencia_Imputada} que registra el valor con el que se ha
sustituido cada cero. Vale \code{NULL} si no habia ceros.
\item \code{nota_imputacion}: cadena de caracteres describiendo el numero
de celdas imputadas (o indicando que no habia ceros).
}

Utiliza internamente \code{\link[=detect_zeros_any]{detect_zeros_any()}} para localizar las celdas
afectadas.
}
\examples{
\dontrun{
# Tabla de contingencia con ceros
tab <- xtabs(~ GALAXIA + FJUR + EFLO, data = seleccion)

# Imputar ceros con 1
tab_use <- impute_zeros_with_one(tab, value = 1L)

# Consultar la nota y el detalle de las celdas imputadas
cat(attr(tab_use, "nota_imputacion"), "\n")
attr(tab_use, "zeros_imputados")
}

}
