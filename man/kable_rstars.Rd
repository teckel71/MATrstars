% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/kable_rstars.R
\name{kable_rstars}
\alias{kable_rstars}
\title{Presentar una tabla con el estilo del libro R-Stars}
\usage{
kable_rstars(
  x,
  caption = NULL,
  col.names = NULL,
  digits = getOption("digits"),
  font_size = 11,
  full_width = FALSE,
  bootstrap_options = c("striped", "bordered", "condensed"),
  align = "c",
  align_header = "c",
  bold_header = TRUE,
  bold_body = FALSE,
  format.args = list(decimal.mark = ".", scientific = FALSE),
  ...
)
}
\arguments{
\item{x}{Data frame o matriz con los datos a mostrar.}

\item{caption}{Título de la tabla. Por defecto \code{NULL} (sin título).}

\item{col.names}{Vector de caracteres con los nombres de columna a mostrar.
Por defecto \code{NULL} (se usan los nombres del data frame).}

\item{digits}{Número de dígitos decimales. Puede ser un escalar (aplicado
a todas las columnas numéricas) o un vector con un valor por columna
(\code{NA} para columnas no numéricas). Por defecto, el valor de
\code{getOption("digits")}.}

\item{font_size}{Tamaño de fuente en puntos. Por defecto \code{11}.}

\item{full_width}{Lógico. Si \code{TRUE}, la tabla ocupa todo el ancho
disponible. Por defecto \code{FALSE}.}

\item{bootstrap_options}{Vector de caracteres con opciones de estilo de
Bootstrap para \code{kable_styling()}. Por defecto
\code{c("striped", "bordered", "condensed")}.}

\item{align}{Alineación de las celdas del cuerpo. Por defecto \code{"c"}
(centrado). Ver \code{kableExtra::row_spec()}.}

\item{align_header}{Alineación de las celdas del encabezado. Por defecto
\code{"c"} (centrado).}

\item{bold_header}{Lógico. Si \code{TRUE} (por defecto), el encabezado va en
negrita.}

\item{bold_body}{Lógico. Si \code{TRUE}, el cuerpo va en negrita. Por defecto
\code{FALSE}.}

\item{format.args}{Lista con argumentos de formato numérico pasada a
\code{knitr::kable()}. Por defecto, punto como separador decimal y
notación científica desactivada.}

\item{...}{Argumentos adicionales pasados a \code{knitr::kable()}.}
}
\value{
Un objeto de clase \code{knitr_kable} con el estilo del libro
aplicado, listo para su inclusión en un documento R Markdown con
salida HTML.
}
\description{
Genera una tabla HTML aplicando el estilo tipográfico y de color
utilizado sistemáticamente en el libro \emph{R-Stars} (encabezado en negrita
centrado, cuerpo con líneas alternas, bordes discretos, ancho ajustado,
cuerpo centrado). Es un envoltorio de \code{knitr::kable()} seguido de
\code{kableExtra::kable_styling()} y \code{kableExtra::row_spec()} con los ajustes
habituales del libro; los defaults resuelven el 95\% de las tablas, y el
resto se puede personalizar sobrescribiendo los argumentos correspondientes.
}
\details{
Sustituye el bloque repetitivo de 12-18 líneas que aparece en cada tabla
de los \emph{scripts} del libro por una única llamada compacta. Además, elimina
de raíz un error frecuente que aparecía en varias tablas (los tres
\code{bootstrap_options} sin envolver en \code{c(...)}, lo que hacía que el estilo
solo aplicara \code{"striped"} y descartara los otros dos).
}
\examples{
\dontrun{
library(dplyr)
library(MATrstars)

# Ejemplo con caption y col.names personalizados
mtcars \%>\%
  head(5) \%>\%
  kable_rstars(caption   = "Primeros 5 coches del dataset mtcars",
               col.names = c("mpg", "cil", "cc", "hp", "gr", "peso",
                             "1/4 mi", "vs", "am", "marchas", "carbs"),
               digits    = 2)
}

}
