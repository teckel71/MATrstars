% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/extraer_coeficientes.R
\name{extraer_coeficientes}
\alias{extraer_coeficientes}
\title{Extraer los coeficientes de un modelo log-lineal}
\usage{
extraer_coeficientes(modelo)
}
\arguments{
\item{modelo}{Modelo log-lineal ajustado (habitualmente devuelto por
\code{\link[MASS:loglm]{MASS::loglm()}}).}
}
\value{
Un data frame con dos columnas:
\itemize{
\item \code{Coefficient}: nombre del coeficiente.
\item \code{Value}: valor estimado del coeficiente.
}
}
\description{
Toma un modelo log-lineal ajustado (habitualmente devuelto por
\code{\link[MASS:loglm]{MASS::loglm()}}) y descompone sus parametros (\code{modelo$param}) en un
data frame plano con dos columnas: \code{Coefficient} (nombre) y \code{Value}
(valor estimado). El nombre se construye combinando el termino y los
niveles asociados, separados por \code{":"}, para facilitar la lectura de
modelos con multiples interacciones.
}
\details{
La funcion contempla los distintos casos que puede presentar
\code{modelo$param}:
\itemize{
\item Intercepto: se etiqueta como \code{"T. Independiente"}.
\item Efectos principales: el nombre resulta de \code{variable:nivel}.
\item Interacciones dobles (matrices): se recorren filas y columnas,
etiquetando como \code{nivel_fila:nivel_columna}.
\item Interacciones triples (arrays 3D): se recorren las tres
dimensiones, etiquetando como \code{nivel1:nivel2:nivel3}.
}

Se utiliza internamente en \code{\link[=generar_solucion]{generar_solucion()}}, pero se expone como
funcion publica del paquete para permitir su uso independiente si el
usuario quiere construir su propia presentacion de resultados.
}
\examples{
\dontrun{
modelo <- MASS::loglm(~ GALAXIA + FJUR + EFLO, data = tab_use)
extraer_coeficientes(modelo)
}

}
