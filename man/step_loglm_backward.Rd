% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/step_loglm_backward.R
\name{step_loglm_backward}
\alias{step_loglm_backward}
\title{Seleccion backward por AIC para modelos log-lineales}
\usage{
step_loglm_backward(formula, data, trace = TRUE, steps = 1000)
}
\arguments{
\item{formula}{Formula del modelo inicial (habitualmente el modelo
saturado, como \code{~ A * B * C}).}

\item{data}{Tabla de contingencia sobre la que se ajusta el modelo
(objeto \code{table}, \code{array} o \code{matrix} de cualquier numero de
dimensiones, compatible con \code{\link[MASS:loglm]{MASS::loglm()}}).}

\item{trace}{Si \code{TRUE} (por defecto), imprime en la consola cada
paso del algoritmo con el AIC actual, los terminos candidatos y
sus respectivos AIC.}

\item{steps}{Numero maximo de iteraciones. Por defecto, \code{1000}. En
la practica, la seleccion termina mucho antes al no encontrar mas
terminos que mejoren el AIC.}
}
\value{
Objeto de clase \code{loglm} correspondiente al modelo final
seleccionado, ajustado con \code{fitted = TRUE} (por lo que incluye las
frecuencias ajustadas necesarias para diagnostico posterior).
}
\description{
Implementa un procedimiento de seleccion de modelos log-lineales por
eliminacion progresiva de terminos (\emph{backward}) usando el criterio de
informacion de Akaike (AIC). Es una alternativa a \code{\link[stats:step]{stats::step()}}
pensada especificamente para modelos ajustados con \code{\link[MASS:loglm]{MASS::loglm()}}.
}
\details{
La motivacion para no usar \code{\link[stats:step]{stats::step()}} es puramente tecnica:
\code{step()} localiza la tabla de datos de cada submodelo mediante
\code{eval.parent()}, que sube por la pila de llamadas hasta encontrarla.
Este mecanismo funciona en una sesion interactiva, pero es fragil
cuando el codigo se ejecuta a traves de varias capas de funciones
envolventes (por ejemplo, al compilar un libro con
\code{bookdown::render_book()} desde una funcion como \code{build_book()}),
porque el numero de saltos en la pila cambia y \code{step()} deja de
localizar la tabla, produciendo un error dificil de diagnosticar.

Esta funcion reimplementa el algoritmo \emph{backward} por AIC sin
depender de \code{eval.parent()}: la tabla de datos se pasa como argumento
explicito y queda capturada en el \emph{closure} de una funcion auxiliar
interna, resolviendose por alcance lexico (no dinamico). El resultado
es equivalente al de \code{step()} en sesion interactiva, pero es robusto
en cualquier contexto de ejecucion (consola, \code{knitr}, \code{bookdown},
\code{pkgdown}, funciones envolventes, etc.).

Internamente, en cada iteracion:
\enumerate{
\item Se identifican los terminos candidatos a eliminar mediante
\code{\link[stats:factor.scope]{stats::drop.scope()}} aplicado a los terminos de la formula
actual (esto respeta la jerarquia: no se elimina un efecto
principal si esta implicado en una interaccion todavia
presente en el modelo).
\item Se ajusta un submodelo por cada termino candidato y se
calcula su AIC mediante \code{\link[stats:extractAIC]{stats::extractAIC()}}.
\item Se selecciona el termino cuya eliminacion produce el menor
AIC. Si el AIC minimo no mejora el actual, el algoritmo
termina y devuelve el modelo actual.
}
}
\examples{
\dontrun{
# Partiendo del modelo saturado sobre una tabla de contingencia:
tab0 <- xtabs(~ GALAXIA + FJUR + EFLO, data = seleccion)

modelo_def <- step_loglm_backward(~ GALAXIA * EFLO * FJUR,
                                  data  = tab0,
                                  trace = TRUE,
                                  steps = 1000)

# El resultado es un objeto loglm equivalente al de step()
# pero robusto en contextos de compilacion:
generar_solucion(modelo_def)
}

}
