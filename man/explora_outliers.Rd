% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/explora_outliers.R
\name{explora_outliers}
\alias{explora_outliers}
\title{Explora (y opcionalmente elimina) outliers univariantes o multivariantes.}
\usage{
explora_outliers(
  datos,
  variables = NULL,
  accion = c("documentar", "eliminar"),
  titulo = NULL,
  subtitulo = NULL,
  escala = c("auto", "lineal", "pseudo_log"),
  caption = NULL,
  ...
)
}
\arguments{
\item{datos}{Data frame de entrada.}

\item{variables}{Variables a analizar (tidy-select). Si se omite, se
analizan todas las columnas de \code{datos} que sean
numericas.}

\item{accion}{\code{"documentar"} (por defecto) solo diagnostica.
\code{"eliminar"} ademas filtra los casos identificados
como outliers.}

\item{titulo}{Titulo del grafico. Si es \code{NULL} (por defecto), se
genera automaticamente segun sea univariante o
multivariante.}

\item{subtitulo}{Subtitulo del grafico. Por defecto, \code{NULL}.}

\item{escala}{\code{"auto"} (por defecto): detecta automaticamente si la
caja se comprime y aplica escala pseudo-logaritmica.
\code{"lineal"}: siempre escala lineal.
\code{"pseudo_log"}: siempre escala pseudo-logaritmica.}

\item{caption}{Titulo de la tabla \code{kable_rstars()} con los casos
outliers. Si es \code{NULL} (por defecto), se genera
automaticamente.}

\item{...}{Argumentos adicionales reenviados a \code{\link[=kable_rstars]{kable_rstars()}}
(por ejemplo, \code{digits}, \code{font_size} o
\code{bootstrap_options}).}
}
\value{
El data frame (invisible): sin cambios si
\code{accion = "documentar"}, o filtrado (sin los casos outliers) si
\code{accion = "eliminar"}. En el caso multivariante, la columna
auxiliar MAHALANOBIS que se calcula internamente NO se
conserva en el data frame devuelto.
}
\description{
Diagnostica los outliers (valores atipicos) de un data frame sobre un
conjunto de variables metricas, siguiendo la regla clasica de
Tukey de 1.5 x IQR:
}
\details{
\itemize{
\item Si se pasa \strong{una sola} variable, el analisis se hace directamente
sobre ella.
\item Si se pasan \strong{dos o mas}, se calcula la \strong{distancia de Mahalanobis}
de cada caso y el analisis se hace sobre esa medida sintetica.
}

En ambos casos, genera un grafico \code{ggplot2::geom_boxplot()} con el
estilo del libro y, si se detectan outliers, una tabla con los casos
afectados formateada con \code{kable_rstars()}. Si no se detecta ningun
outlier, la funcion solo emite un mensaje informativo. Opcionalmente,
elimina del data frame los casos identificados como outliers.

La seleccion de variables sigue el estilo \emph{tidy-select} del \emph{tidyverse}
(idem que en \code{\link[=explora_na]{explora_na()}}).
}
\section{Variables no metricas}{
Si alguna de las variables seleccionadas no es metrica (por ejemplo,
un factor o una cadena), la funcion emite un aviso y no realiza el
analisis, devolviendo el data frame sin cambios. El usuario debe
seleccionar solo variables numericas.
}

\section{Caja "aplastada" por outliers extremos}{
Cuando existen outliers muy extremos, la caja del boxplot se comprime
y practicamente desaparece. Para mitigarlo, la funcion detecta
automaticamente esta situacion (cuando el IQR ocupa menos del 10\% del
rango total de la variable) y transforma el eje Y con una escala
pseudo-logaritmica (\code{scales::pseudo_log_trans()}), que se comporta
como una escala lineal cerca de 0 y como una logaritmica en los
extremos. Esta transformacion funciona incluso con valores negativos o
nulos, a diferencia de \code{log10()}. El subtitulo del grafico indica
cuando se ha aplicado.

El comportamiento se controla con el argumento \code{escala}:
\itemize{
\item \code{"auto"} (por defecto): detecta y aplica pseudo-log si es necesario.
\item \code{"lineal"}: fuerza escala lineal (comportamiento clasico).
\item \code{"pseudo_log"}: fuerza escala pseudo-logaritmica.
}
}

\section{Integracion con el preprocesador del libro R-Stars}{
Igual que \code{\link[=explora_na]{explora_na()}}, cuando la funcion se ejecuta dentro de un
documento R Markdown / knitr, emite el grafico y la tabla con el
estilo tipografico habitual e inserta ademas las etiquetas manuales
"\strong{Figura X.Y}" y "\strong{Tabla X.Y}" justo debajo de cada uno, leyendo
los numeros de las opciones \code{matrstars.figura_num} y
\code{matrstars.tabla_num} que el preprocesador \code{numerar_tablas_figuras.R}
inyecta. El chunk debe llevar \code{results='asis'}.
}

\examples{
\dontrun{
  library(MATrstars)

  # Univariante:
  muestra_so <- explora_outliers(
    muestra, RENECO,
    accion = "eliminar",
    titulo = "Rentabilidad Economica"
  )

  # Multivariante (via distancia de Mahalanobis):
  muestra2_so <- explora_outliers(
    muestra2, c(RENECO, ACTIVO, MARGEN, RES),
    accion = "eliminar"
  )

  # Todo el df (todas las columnas numericas):
  seleccion_so <- explora_outliers(seleccion, accion = "eliminar")
}

}
